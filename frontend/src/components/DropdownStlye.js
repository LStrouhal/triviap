import {makeStyles} from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    formControl: {minWidth: 200, background: "#f7f7f2", fontSize: "0.7em"},
    MenuItem: {fontSize: "0.7em"}
}))

export default useStyles;